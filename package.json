{
  "version": "0.1.1",
  "private": true,
  "license": "MIT",
  "scripts": {
    "prevbuild": "yarn && npm run build:dist",
    "frontend:dev": "yarn workspace @hh/frontend dev",
    "backend:dev": "yarn workspace @hh/backend dev",
    "build": "lerna run build",
    "build:dist": "lerna run build:dist",
    "build:full": "yarn build:dist && yarn build",
    "jest": "lerna run jest",
    "generated": "yarn workspace @hh/generated generate",
    "generated:watch": "yarn workspace @hh/generated generate:watch"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "resolutions": {
    "typescript": "^4.0.5",
    "@apollo/client": "^3.2.5",
    "@types/node": "^14.14.6"
  },
  "workspaces": [
    "frontend",
    "backend",
    "common/**/*",
    "generated",
    "generated/**/*"
  ]
}
