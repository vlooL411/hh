query Vacancy($id: ID!) {
  vacancy(id: $id) {
    id
    ...VacancyInfoDescription
  }
}

query Vacancies($page: Int) {
  vacancies(page: $page) {
    per_page
    page
    pages
    found
    items {
      id
      ...VacancyInfoSnippet
    }
  }
}
