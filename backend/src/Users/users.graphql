interface IUser {
	userId: ID!
	username: String!
}

type UserSafe implements IUser {
	userId: ID!
	username: String!
}

type User implements IUser {
	userId: ID!
	username: String!
	password: String!
}
